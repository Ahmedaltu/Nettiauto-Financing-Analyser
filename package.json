{
  "name": "nettiauto-financing-analyser-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright UI test suite for Nettiauto Financing Analyser Chrome extension",
  "scripts": {
    "manifest:sync:test": "node scripts/sync-test-manifest.js",
    "release:zip": "npm run manifest:sync:test && node scripts/build-release-zip.js",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "typescript": "^5.8.3"
  }
}
